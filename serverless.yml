service: appsync-with-cognito
frameworkVersion: '2'

provider:
  name: aws
  runtime: python3.8
  lambdaHashingVersion: 20201221
  stage: ${opt:stage, 'dev'}

functions:
  hello:
    handler: handler.hello
    
resources:
  Resources:
    ${file(resources.yml)}